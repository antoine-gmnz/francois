// Pi retirement: behavioral override; canonical IPC shapes remain in their existing domains.
// Resolved Pi targets on executing/mutating commands fail RUNTIME_UNSUPPORTED before side effects.
// Read/list/local transcript projection remains available; native Pi files are never opened.
// Retained Pi discriminators and saved fields are compatibility data, never execution capability.
export type { AgentRuntime, AppError, Result, SessionMeta, SessionEvent } from './common';
export type { Account, AccountKind } from './multi-account';
export type { SessionProfile } from './session-profiles';
