# Francois

Read `PIPELINE.md` for the project profile, surfaces, conventions and commands.
Read `CLAUDE.md` for the shared project overview. Feature specs live in `specs/<id>.md`.

Cohorte uses the global Codex core at `~/.cohorte/codex`; project state is in
`.cohorte/`, surface agents in `.codex/agents/`, and MCP configuration in
`.codex/config.toml`. Update with `$cohorte-update-pipeline`. Keep the normal
user `CODEX_HOME`; no project authentication or session files belong in git.
