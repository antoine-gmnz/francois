/// <reference types="vite/client" />

/**
 * Substituted with a literal `true`/`false` by Vite's `define` (vite.config.ts)
 * from VITE_FRANCOIS_DEMO. `true` enables the demo backend in `src/demo/`, which
 * exists only to capture the README screenshot and GIF; `false` makes every use
 * of it dead code, so it never reaches a shipped build.
 */
declare const __FRANCOIS_DEMO__: boolean;
